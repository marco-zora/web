@page "/impianti"
@inject HttpClient Http
@inject IJSRuntime JS
@inject NavigationManager Nav


<!-- <h3 class="mt-3 mb-4">Gestione Impianti</h3> -->


@if (!Loaded)
{
    <p>Caricamento…</p>
    return;
}

@if (Error is not null)
{
    <div class="alert alert-danger">@Error</div>
    return;
}

<!-- Vista ADMIN -->
@if (LoggedRole == "admin")
{
    <h2 class="mb-4 fw-bold text-primary">
        <i class="bi bi-collection"></i> Impianti di Tutti i Clienti
    </h2>

    @foreach (var cliente in Root!.Keys.OrderBy(k => k))
    {
        var impiantiCliente = Root[cliente].impianti
                                          .OrderBy(i => i.nome)
                                          .ToList();

        <div class="mb-5 pb-3 border-bottom">

            <!-- Header Cliente -->
            <div class="d-flex justify-content-between align-items-center mb-3">
                <h3 class="fw-bold text-dark m-0">
                    <i class="bi bi-person-circle"></i> @cliente
                </h3>

                @*
                <button class="btn btn-success btn-sm shadow-sm"
                        @onclick="() => AddImpianto(cliente)">
                    <i class="bi bi-plus-circle"></i> Nuovo Impianto
                </button>
                
                <button class="button-a" style="display:flex; justify-content:flex-end;" @onclick="() => AddImpianto(cliente)">
                
                *@
                
                <button class="button-a" @onclick="() => AddImpianto(cliente)">
                    Nuovo Impianto 
                </button>

            </div>

            @if (impiantiCliente.Count == 0)
            {
                <p class="text-muted fst-italic">Nessun impianto per questo cliente.</p>
            }
            else
            {
                <!-- Griglia a wrap con cards a larghezza fissa -->
                <div class="impianti-grid">
                    @foreach (var imp in impiantiCliente)
                    {
                        <div class="impianto-cell">
                            <ImpiantoCard Impianto="imp"
                                          ShowButtonLabels="true"
                                          OnDettaglio="() => ApriDettaglio(cliente, imp)"
                                          OnRinomina="() => RenameImpianto(cliente, imp)"
                                          OnElimina="() => DeleteImpianto(cliente, imp)" />
                        </div>
                    }
                </div>               
            }

        </div>
    }
}
else
{
    <!-- Vista USER -->
    <div class="d-flex justify-content-between align-items-center mb-3">
        <h4 class="text-primary">@LoggedUser</h4>

        <button class="btn btn-sm btn-success"
                @onclick="() => AddImpianto(LoggedUser!)">
            <i class="bi bi-plus-circle"></i> Nuovo Impianto
        </button>
    </div>


    <div class="impianti-grid">
        @foreach (var imp in UserData!.impianti.OrderBy(i => i.nome))
        {
            <div class="impianto-cell">
                <ImpiantoCard Impianto="imp"
                              ShowButtonLabels="true"
                              OnDettaglio="() => ApriDettaglio(LoggedUser!, imp)"
                              OnRinomina="() => RenameImpianto(LoggedUser!, imp)"
                              OnElimina="() => DeleteImpianto(LoggedUser!, imp)" />
            </div>
        }
    </div>

}

@code {
    //private string ProxyBase = "https://first.marcomaria-zora.workers.dev/api/dav/UPLOADS/";
    //private const string FileName = "impianti.json";

    private string? LoggedUser;
    private string? LoggedRole;

    private ImpiantiRoot? Root;
    private UserImpianti? UserData;
    private List<UserRecord>? UsersList;

    private bool Loaded = false;
    private string? Error;

    protected override async Task OnInitializedAsync()
    {
        LoggedUser = await JS.InvokeAsync<string>("sessionStorage.getItem", "loggedUser");
        LoggedRole = await JS.InvokeAsync<string>("sessionStorage.getItem", "loggedRole");

        if (string.IsNullOrWhiteSpace(LoggedUser))
        {
            Nav.NavigateTo("login");
            return;
        }

        await LoadImpianti();
        Loaded = true;
    }

    private async Task LoadImpianti()
    {
        try
        {
            // 1) impianti.json
            Root = await Http.GetFromJsonAsync<ImpiantiRoot>(Global.ProxyBase + Global.InstallationFileName);
            Root ??= new ImpiantiRoot();

            // default colore se manca (retrocompatibilità)
            foreach (var kv in Root)
                foreach (var imp in kv.Value.impianti)
                    imp.colore ??= "#e3f2fd";

            // 2) users.json: aggiungi anche utenti senza impianti
            UsersList = await Http.GetFromJsonAsync<List<UserRecord>>(Global.ProxyBase + Global.UsersFileName);
            UsersList ??= new List<UserRecord>();

            foreach (var u in UsersList)
            {
                if (!Root.ContainsKey(u.user))
                    Root[u.user] = new UserImpianti();
            }

            // 3) Vista user
            if (!string.Equals(LoggedRole, "admin", StringComparison.OrdinalIgnoreCase))
            {
                if (!Root.ContainsKey(LoggedUser!))
                    Root[LoggedUser!] = new UserImpianti();

                UserData = Root[LoggedUser!];
            }
        }
        catch (Exception ex)
        {
            Error = $"Errore caricamento impianti: {ex.Message}";
            Root ??= new ImpiantiRoot();
        }
    }

    private async Task Save()
    {
        var json = System.Text.Json.JsonSerializer.Serialize(
            Root,
            new System.Text.Json.JsonSerializerOptions { WriteIndented = true }
        );

        using var content = new StringContent(json, System.Text.Encoding.UTF8, "application/json");
        await Http.PutAsync(Global.ProxyBase + Global.InstallationFileName, content);
    }

    private async Task AddImpianto(string user)
    {
        var name = await JS.InvokeAsync<string?>("prompt", "Nome nuovo impianto:");
        if (string.IsNullOrWhiteSpace(name)) return;

        Root![user].impianti.Add(new Impianto
        {
            id = Guid.NewGuid().ToString("N"),
            nome = name.Trim(),
            colore = "#e3f2fd"
        });

        await Save();
    }

    private async Task RenameImpianto(string user, Impianto imp)
    {
        var newName = await JS.InvokeAsync<string?>("prompt", "Nuovo nome:", imp.nome);
        if (string.IsNullOrWhiteSpace(newName)) return;

        imp.nome = newName.Trim();
        await Save();
    }

    private async Task DeleteImpianto(string user, Impianto imp)
    {
        var ok = await JS.InvokeAsync<bool>("confirm", $"Eliminare '{imp.nome}'?");
        if (!ok) return;

        Root![user].impianti.Remove(imp);
        await Save();
    }

    private void ApriDettaglio(string user, Impianto imp)
    {
        Nav.NavigateTo($"impianto/{user}/{imp.id}");
    }
}